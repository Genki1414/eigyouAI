# 本番で必要なもの
anthropic>=0.40.0        # エンリッチメント・文面生成
scikit-learn>=1.4        # スコアV2の学習
numpy>=1.26
psycopg[binary]>=3.1     # Postgresへ移行する場合のみ
# メール送信はResend(2026-09-09にSendGridから切替)。REST APIを標準ライブラリの
# urllibで直接叩くだけなので専用SDKは不要(senders.py MailSender._deliver()参照)
openpyxl>=3.1            # 都道府県別建設業許可業者名簿(Excel)の取込
playwright>=1.40         # FormSender(問い合わせフォーム自動送信)。別途 `playwright install --with-deps chromium` が必要
