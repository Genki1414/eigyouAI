# 引き継ぎパッケージ — 足場AI営業エンジン

**最終更新**: 2026年7月31日
**状態**: プロトタイプ完成・全テスト通過・本番接続待ち
**次の担当**: Claude Code

---

## まず読む順番

1. **この INDEX.md**（全体像とファイル一覧）
2. **HANDOFF.md**（実装タスクT1〜T7・やってはいけないこと）
3. **README.md**（Phase 1〜7の全開発経緯と設計思想）
4. 動かす: `python3 run.py all --demo` → `python3 test_pipeline.py`

---

## ⚠ 最重要の注意（先に読むこと）

**現在の全数値はシミュレーションです。**
CAC・反応率・転換率・LTVはすべて `campaign.py` の `simulate()` が
仮置きパラメータから生成した値で、実測は1件もありません。
IM.md・console.html に出ている数字を、そのまま買い手や顧客に見せないこと。
実データが入れば同じコマンドで実測値に置き換わります(console.htmlは現在、
`python3 console.py` が `out/companies.db` から都度生成する方式に変更済み。
実送信がまだ無ければ「準備中」表示になり、シミュレーション値は出ない)。

**LPの「使っていただいている声」は創作です。**
`lp.html` の voice セクションは実在の導入企業ができるまで削除すること。

**法令はテスト済みではありません。**
特定電子メール法まわりは実装で配慮していますが、
法人の公開アドレス宛の扱いは弁護士に確認してください。

---

## ファイル一覧

### 中核パイプライン（実行順）
| ファイル | 役割 | 本番で触るか |
|---|---|---|
| `config.py` | 全設定の単一情報源（閾値・単価・法令設定） | 値の調整のみ |
| `db.py` | スキーマ / マイグレーション / **接触ガード** | 触らない |
| `storage.py` | SQLite⇔Postgres 方言吸収 | 移行時のみ |
| `resilience.py` | レート制御 / 再試行 / 再開 / 冪等 | 触らない |
| `ingest.py` | 都道府県別許可業者名簿Excel取込（県ごとの差は`parsers/<pref>.py`） | **T1で実装** |
| `ingest_mikomeru.py` | mikomeru CSV取込（既存社にはURL情報のみ書き足し・新規は追加） | **T9で実装** |
| `prescore.py` | AI不要の事前絞込（資本金/連番/層化サンプリング） | 触らない |
| `enrich.py` | AIエンリッチメント（HP/求人/レビュー） | APIキー設定のみ |
| `offers.py` | テナント・オファー定義（マルチプロダクト）+ `add-tenant`でSaaS販売先を追加 | オファー追加時 |
| `target_lists.py` | テナントごとの送信先リスト作成（フィルタ型・CSV取込型） | **T12で実装** |
| `scoring.py` | スコアV1（4軸100点） | 触らない |
| `campaign.py` | キャンペーン作成（ガード適用） | 触らない |
| `compose.py` | AI文面生成 + NGワード検査 | 触らない |
| `senders.py` | 送信アダプタ（FAX/メール/SMS/郵送） | **T2/T5で実装** |
| `api.py` | 反応・課金・停止の受け口 | **T3/T4で接続** |
| `followup.py` | 多段フォロー（D+14/D+35）+ 文面増殖 | 触らない |
| `dormant.py` | 休眠プール（180日）+ 復活シグナル | 触らない |
| `metrics.py` | ファネル / CAC / チャネル別集計 | 触らない |
| `learn.py` | スコアV2学習 + **昇格ゲート** | 触らない |
| `im.py` | M&A用IM自動生成 | 触らない |
| `suppress_cli.py` | 配信停止の受付・監査 | 運用で使う |
| `announcements_cli.py` | お知らせ(全テナント共通告知)の投稿・管理 | 運用で使う |
| `kill_switch_cli.py` | 異常検知時に実送信を即時停止/解除(全体・テナント別) | 運用で使う |
| `cost_report_cli.py` | 1送信あたりの原価・テナント別粗利の集計(管理者専用) | 運用で使う |
| `scheduled_send_cli.py` | 予約送信(MIKOMERU同等)の実行係。5分おきにcronから起動 | 運用で使う |
| `run.py` | オーケストレーター（実行順序を強制） | 触らない |
| `generate_sample.py` | デモ用サンプル3000社生成 | 本番では不要 |

### テスト（全て通る状態を維持すること）
| ファイル | 項目数 | 検証内容 |
|---|---|---|
| `test_pipeline.py` | 48 | 不変条件・法令遵守・数字の整合・モデル精度 |
| `api.py test` | 91 | エンドポイント・署名・冪等・アトリビューション・Stock Factory運用API・テナント向けSaaS API全般(送信先リスト・除外設定・テンプレート・担当者・お知らせ・ログ) |
| `test_concurrency.py` | 5群 | 並列書き込み・レート・再開・二重送信防止 |
| `senders.py test` | 3群 | 宛先検証・送信者表示・冪等 |
| `storage.py test` | 5 | SQL方言変換 |

### 画面
| ファイル | 用途 | 見るべき点 |
|---|---|---|
| `dashboard.html` | リード司令塔 | S/Aランク・推奨チャネル・AI所見 |
| `console.py` → `out/console.html` | キャンペーンコンソール(実データから毎回生成) | ファネル・CAC・A/B・モデル昇格状況・フォーム送信β検証実績 |
| `list_builder.html` | 送信先リスト作成・管理(SaaS顧客向け。テナントAPIキーで接続) | mikomeru.net相当のサイドバー構成。フィルタ絞込・CSV取込・保存済みリストからの送信・除外設定・送信文章/送信元テンプレート・担当者管理・お知らせ・活動ログ・マニュアル |
| `hq.html` | 本部専用(社内運用者向け。`SALES_ENGINE_API_KEY`で接続、T46) | 契約テナントの新規作成・スタッフアカウントの代行発行。顧客向け画面からは一切リンクしない |
| `lp.html` | 無料積算ツールLP | ⚠ 「お客様の声」は要削除 |

### ドキュメント
| ファイル | 内容 |
|---|---|
| `HANDOFF.md` | **実装タスクT1〜T7 / やってはいけないこと / 運用指標** |
| `README.md` | Phase 1〜7の開発経緯・設計思想・意思決定の理由 |
| `INDEX.md` | この文書 |
| `IM.md` | M&A用インフォメーションメモランダム（自動生成物） |

### デプロイ
| ファイル | 内容 |
|---|---|
| `deploy/Dockerfile` | Python 3.12 + cron + ヘルスチェック |
| `deploy/docker-compose.yml` | api / worker / postgres |
| `deploy/crontab` | 定期実行（**送信は平日9時と14時のみ**） |
| `requirements.txt` | 本番で必要なパッケージ |
| `.env.example` | 環境変数テンプレート（全項目を埋めること） |

### データ
| ファイル | 内容 |
|---|---|
| `companies.db` | デモデータ入りSQLite（3000社・全工程実行済み） |
| `metrics.json` | ファネル集計結果 |
| `model_v2.json` | 学習済みモデル（昇格判定含む） |
| `scored.json` | スコアリング結果全件 |

---

## 動作確認（最初の5分）

```bash
pip install -r requirements.txt

python3 run.py status          # 進捗一覧
python3 run.py all --demo      # 全工程をデモデータで実行
python3 test_pipeline.py       # 48項目
python3 api.py test            # 27項目
python3 test_concurrency.py
python3 senders.py test
python3 storage.py test
```

全て通らない状態で先に進まないこと。

---

## この設計で守られている不変条件

- 全送信が `db.can_contact()` を通る（配信停止・重複・反応済み・既存顧客・上限・間隔）
- 反応が返った会社への未送信フォローは自動取消
- 同じ `idem_key` で二度送らない
- 学習モデルはV1を上回った時だけ採用（昇格ゲート）
- 送信者情報はアダプタ層が強制付与
- NGワードは生成後に機械で検査

これらは開発中にテストが実際の漏れを3回検出した結果として入っています
（詳細はREADME.mdのPhase 5〜7）。ガードは「作る」ではなく「全経路が通る」ことが要件です。

---

## 本番までの残作業（HANDOFF.md 参照）

| # | タスク | 工数 |
|---|---|---|
| T1 | 国交省データ取込 | 半日 |
| T2 | メール送信実装 | 半日 |
| T3 | LP接続 | 2時間 |
| T4 | 課金webhook | 2時間 |
| T5 | FAX送信実装 | 半日 |
| T6 | Postgres移行（数万社超えてから） | 半日 |
| T7 | デプロイ | 2時間 |

**合計 約3人日**。ただしT1の名簿Excel取得と法務確認は別途。

---

## スケジュールの現実

目標が2027年3月のM&Aなら、CACが意味のある数字になるまで反応が100件は要ります。
逆算すると**9月中に実送信を開始**しないと、買い手に見せられる実測期間が3〜4ヶ月しか
取れません。T1〜T4を最優先で。T5（FAX）とT6（Postgres）は後回しで構いません。
