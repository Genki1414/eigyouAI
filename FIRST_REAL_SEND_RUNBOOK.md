# 明日の実送信手順書(最初の5社)

**この手順書は、初めて実在企業へフォームを本番送信するための、人間が確認しながら
進める前提の手順です。今夜のセッションでは実在企業への本番送信は一切行っていません。**

対象読者: β版の最初の実送信を行う人間(あなた)。

---

## 0. 前提

- Kill Switchは**既定で「全体停止中」**です(`kill_switch_cli.py`で確認・操作)。
  本番送信するには、送信の直前に人間が明示的に解除する必要があります。
- HTTPS化は完了済みです。`https://app.ashibase.jp/`で`list_builder.html`に
  アクセスします(サーバー上のnginxがTLS終端。詳細はHANDOFF.md T13参照)。
- 以下はすべてサーバー上(SSH接続後)、または`list_builder.html`の画面上で行います。

---

## 1. 現在地の確認(送信前チェックリスト)

サーバーにSSH接続し、プロジェクトディレクトリで:

```bash
# Kill Switchの状態(全体停止中になっているはず)
python3 kill_switch_cli.py status

# 直近の送信ログに異常な状態が残っていないか
python3 -c "
import db
con = db.connect()
print('未確定(処理中のまま止まった)行:',
    con.execute(\"SELECT COUNT(*) FROM target_list_members WHERE send_status='PROCESSING'\").fetchone()[0])
"

# デプロイされているコードが最新か(コミットハッシュを確認)
git log -1 --oneline
```

`list_builder.html`(ブラウザ)でも確認:
- ホーム画面に「今月、AI営業社員が0社へ営業しました」等、実績がまだ0であることを確認
- 上部に「⚠ 現在、本番送信は停止されています」の赤いバナーが出ていることを確認
  (これが出ていなければ誰かが既にKill Switchを解除している。原因を確認してから進める)

---

## 2. 5社を選ぶ

`list_builder.html`の「リスト取得」ページで、条件を絞り込んで**少数(5社程度)**の
リストを作ります。

推奨する絞り込み方:
- スコアランク: `S`のみ(最も反応が期待できる層)
- 都道府県: 1都道府県に絞る(初回はエリアを絞った方が異常時の影響範囲が読みやすい)
- 「問い合わせフォームURL確定済みのみ」に必ずチェックを入れる
  (これが無いと送信対象にならない)

プレビュー件数が5〜10件程度になるよう調整し、リスト名を付けて保存します
(例:「初回実送信_東京都_Sランク_0822」)。

件数がどうしても多い場合は、保存後に「保存済みリスト」の詳細画面で
`?status=`フィルタと合わせて確認し、上位5社だけを対象に送るか、
一旦全部ドライランしてから、成功が見込める5社だけ手動で確認して進めてください。

---

## 3. 文面を準備する

「送信文章テンプレート」ページで、件名・本文を事前に登録しておくと、
送信画面で選ぶだけで入力できます。初回は丁寧で短い文面を推奨します。

「送信元テンプレート」ページで、送信者名・返信先メールアドレスが
正しく設定されていることを確認してください(未設定だと既定値
`AshiBase（足場ベース）` / `info@ashibase.jp` が使われます)。

---

## 4. ドライランで最終確認する

保存したリストの詳細画面を開き、**ドライラン(既定でON)のまま**「送信する」を
押します。実際のサイトへは何も送信されません。

確認すること:
- 対象企業数が想定通り(5社)か
- 「ガードで中止」が出ていないか(出ていたら、その企業は既に配信停止・除外設定
  等の対象になっている。理由を確認する)
- 企業ごとの一覧で、状態が想定通りか(この時点では`send_status`は変わらない。
  ドライランは`target_list_members`の状態に反映されない設計のため)

---

## 5. Kill Switchを解除する(本番送信の直前だけ)

サーバー上で:

```bash
python3 kill_switch_cli.py resume --reason "初回実送信テスト(5社) $(date +%Y-%m-%d)" --by "<あなたの名前>"
python3 kill_switch_cli.py status   # 🟢 全体は稼働中 になっていることを確認
```

`list_builder.html`をリロードし、赤いバナーが消えていることを確認します。

---

## 6. 本番送信する

リスト詳細画面で、ドライランのチェックを**外し**、確認ダイアログ
(「実際の企業サイトへ送信され、取り消せません。よろしいですか?」)で
内容を再確認してから実行します。

**この操作は取り消せません。** 落ち着いて、5社分の送信結果が返ってくるまで
画面を閉じずに待ってください。

---

## 7. 送信直後にKill Switchを再び止める

5社分の送信が完了したら、すぐにKill Switchを再度停止します
(次のステップに進む判断をする前に、意図しない追加送信を防ぐため)。

```bash
python3 kill_switch_cli.py stop --reason "初回5社送信完了。結果確認中" --by "<あなたの名前>"
```

---

## 8. 結果を確認する

`list_builder.html`で確認:
- 「自動送信ログ」ページ: 5社それぞれの結果(SUCCESS/SKIP_*/FAILED_*)とreason
- リスト詳細画面: 企業ごとの状態バッジ(成功/失敗/SKIP)。会社名をクリックすると
  その会社の送信履歴(1件のはず)が見られる
- ホーム画面: 「今月のAI営業社員の働き」に今回の5件が反映されているか

サーバー上でも直接確認できます:

```bash
python3 -c "
import db
con = db.connect()
for r in con.execute('''SELECT company_id, status, reason_code, error_message
        FROM form_send_log ORDER BY id DESC LIMIT 5'''):
    print(dict(r))
"
```

---

## 9. 異常があった場合(即座に止める)

想定外の挙動(大量エラー、意図しない企業への送信、サイト側からの苦情連絡等)に
気づいたら、即座に:

```bash
python3 kill_switch_cli.py stop --reason "異常検知のため緊急停止" --by "<あなたの名前>"
```

これで、手動送信・cron・Stock Factory運用APIのすべての経路が即座に止まります
(dry_runだけは動き続けます)。停止後、原因を確認してから次に進んでください。

---

## 10. 段階的に拡大する(5社 → 20〜30社 → 50社 → 100社)

各段階で、上記4〜9の手順を繰り返します。回を追うごとに:
- 前回の結果(SUCCESS率・FAILED率・SKIP理由の内訳)を確認してから次の件数に進む
- FAILED_RETRYABLEが多い場合は、フォーム検出ロジック(`form_navigator.py`)側の
  対応が必要な可能性がある。無理に件数を増やさない
- 苦情・返信があれば、その会社を「送信除外設定」で個別除外するか、
  内容によっては`suppress_cli.py`で正式に配信停止に入れる

---

## 参考: 用語とログの見方

| 状態(send_status) | 意味 |
|---|---|
| PENDING | まだ送信していない |
| PROCESSING | 送信中(通常は一瞬で次のいずれかに変わる。残っていたら異常の可能性) |
| SUCCESS | フォーム送信に成功したと判定された |
| SKIP | 対象外(CAPTCHA・営業禁止ページ等。会社自体がダメなわけではない) |
| FAILED_RETRYABLE | 一時的な失敗(タイムアウト等)。後で再送信すれば通る可能性がある |
| FAILED_UNSUPPORTED | フォーム構造が未対応(必須項目が特殊、等) |
| STOPPED | Kill Switchで送信が止められた |

`reason_code`の詳細は`form_send_log.status`(SKIP_CAPTCHA等の具体的なコード)を参照。
